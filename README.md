# 구술기록 서비스 개편 프로토타입 (1단계)

- 목적: 국회기록원 홈페이지 구술콘텐츠 개편안의 원장 보고용 동작 프로토타입
- 기준 문서: 구술콘텐츠_서비스_기획설계서_v3.md, 노션 「구술콘텐츠 서비스 개편 — 현황 분석·벤치마킹·기획안」 8장(지명·인명 사전)
- 작성: 2026. 9. 14.
- 1차 과제(홈·국회의원 컬렉션) 프로토타입과는 코드가 독립적이며, 디자인 토큰(색·서체·카드 문법)만 같은 값을 씀

## 시연 방법

1. `시연하기.command`를 더블클릭함(처음에는 우클릭 → 열기). 브라우저가 `http://localhost:8000/`으로 열림
2. Google 지도를 쓰려면 `config.js`의 `googleMapsApiKey`에 키를 넣음. 키가 없으면 지도 화면이 권역별 간이 좌표도로 바뀜
   - 키는 HTTP 리퍼러를 `http://localhost:*/*`로 제한하여 발급함
3. `index.html`을 파일로 바로 열어도 대부분 동작하지만(`data/bundle.js` 사용), 자막(WebVTT)과 Google 지도는 서버로 열어야 동작함

## 표본 데이터 원칙

- v1.1에서 구술자 8명을 더해 모두 11명, 채록 14회로 늘림. 정리 중·방문 열람 구술자 6명은 세그먼트 없이 채록 정보와 요약만 있음
- 강윤서·안태경은 1차 국회의원 컬렉션(제22대, 가상)의 의원과 같은 인물이며, 강윤서 구술은 1차 컬렉션 서술과 맞춘 '기증자 구술' 표본임
- 구술자 11명, 면담자, 언급 인물, 발언 내용은 모두 **가상**임. 실존 인물의 이름이나 발언은 쓰지 않음(인수인계서 5장)
- 지명은 실재 장소이나 연결된 녹취문은 표본임
- 영상은 녹취문을 macOS 합성 음성(Yuna)으로 읽어 만든 **표본 영상**이며, 화면에 '표본 영상 · 가상 인물 · 합성 음성'을 표시함
- 모든 화면 상단 안내 띠와 왼쪽 아래 '표본 데이터' 표지를 둠

### 1차 국회의원 컬렉션과의 연결

- 1차 프로토타입(claude.ai 「국회기록원 아카이브」)의 기존 콘텐츠는 그대로 두고, 의원 상세(SC-02) 인물 소개 아래에 「구술기록」 연결 상자 하나만 추가함
- 구술자인 의원(강윤서 → `#/narrator/n10`, 안태경 → `#/narrator/n11`)에게만 나타나며, 구술자가 아닌 의원에게는 표시하지 않음
- 연결 근거: 인명 사전 `persons.json`의 `links.member_collection_id`가 1차 컬렉션의 의원 식별자(22-000001 등)와 같음
- 다른 PC에서도 오갈 수 있도록 1차 국회의원 컬렉션 화면을 내용 변경 없이 `collection/index.html`에 함께 둠. 구술기록 상단 「국회의원 컬렉션」 메뉴, 인명 사전의 [국회의원 컬렉션에서 보기], 구술자 상세의 관련 자료 카드가 이 화면으로 연결됨
  - 바로 가기 주소: `collection/index.html#list`(의원 목록), `collection/index.html#member/22-000001`(의원 상세)

## 구현 화면

- v1.1(2026. 9. 15., 원장 데모 후 보완): OH-02 구술자 카드 목록, 구술기록 검색(조건 검색), 색인·구술 안 검색·영문 병기(OHMS 벤치마킹), 1차 국회의원 컬렉션에서 구술자인 의원을 구술콘텐츠로 연결

| 코드 | 화면 | 경로 | 확인할 점 |
| --- | --- | --- | --- |
| OH-01 | 구술기록 홈 | `#/` | 오늘의 증언(자동 재생 없음), 주제 칩, 같은 사건 다른 기억, 구술자, 장소·인물 진입, 최근 공개 |
| OH-02 | 구술자 목록 | `#/narrators` | 국회의원 컬렉션과 같은 카드 문법(사진·성명·직위), 온라인 공개 여부는 사진 색으로만 구분, 구분 탭·초성·검색·정렬, 카드에 초점을 두면 대표 인용 |
| OH-03 | 구술자 상세 | `#/narrator/n01` | 서술형 소개, 대표 인용, 회차별 세그먼트 목록, 구술 속 장소·인물, 다른 구술 속의 이 구술자, 관련 자료, 공개 안내 |
| OH-04 | 세그먼트 재생 | `#/segment/n01-s1-03` | 영상과 녹취문 동기, 문단 클릭 이동, 원본 안의 구간 표시, 이어 재생, 장소·인물 사전 카드, 인용 표기, 내려받기 |
| OH-05 | 주제로 보는 구술 | `#/explore` | 주제별·사건별·시대별 탭, 교차 증언 병렬 배치 |
| 검색 | 구술기록 검색 | `#/search?q=회의록` | 구술자·구분·면담자·구술 장소·채록 사업·매체·면담 일자·공개 상태·주제·사건·다룬 시기·언급 인물·언급 장소 조건, 조건별 건수, 채록 단위 표 / 세그먼트 단위 결과 전환 |
| OH-07 | 지도로 보는 구술 | `#/map` | 지명 사전 핀, 핀별 언급 대목, 좌표 정밀도 표시, 구술자·주제 필터 |
| OH-08 | 인명 사전 | `#/persons`, `#/person/P-N01` | 동일인 묶음, 호칭 이형 표기, 동명이인 구분(이경수), 구술자별 교차 배치 |

- OH-06(구술 이야기)은 2단계임
- 메뉴 구조: 상단 구술기록 메뉴(홈·구술자·주제로 보기·지도로 보기·인명 사전·구술기록 검색)가 유일한 1차 메뉴임. 페이지 안 탭은 「주제로 보기」의 보기 방식(주제별·사건별·시대별)에만 둠

### 공개 수준 4단계 시연 위치

| 공개 수준 | 예시 | 화면 처리 |
| --- | --- | --- |
| 전면 공개 | 홍석진 전체, 한정희 전체 | 원본 영상의 타임코드 구간 재생 + 동기 녹취문 |
| 하이라이트 공개 | `#/segment/n02-s1-02` | 하이라이트 구간만 별도 물리 클립으로 재생(설계 원칙 7의 예외) |
| 녹취문 공개 | `#/segment/n02-s1-03` | 영상 자리에 방문 열람 안내, 녹취문만 제공(재생 이동 없음) |
| 방문 열람 | `#/segment/n02-s1-05` | 제목·요약만 게시. 녹취문은 공개 데이터에 싣지 않음. 지명·인명 연결에서도 제외 |

### 교차 증언이 드러나는 대목(표본 데이터에 의도적으로 넣음)

- 1975년 여의도 이전: 홍석진 1-2 ↔ 한정희 1-2 (같은 사무처 직원 최동환을 둘 다 언급)
- 회의록 수정 요청: 홍석진 1-4 ↔ 한정희 1-3 (같은 일을 의원과 속기사가 각자 기억)
- 1988년 원 구성 협상: 홍석진 1-3 ↔ 윤미경 1-3 (협상장 안과 복도 밖)
- 동명이인: 이경수 의원(P-006)과 이경수 주사(P-007) — 한정희 1-4에서 두 사람이 함께 언급됨

## 폴더 구성

```
index.html, config.js, 시연하기.command
collection/ 1차 국회의원 컬렉션 화면(사본, 구술 연결 상자만 추가)
assets/   app.css, app.js(화면 렌더링), logo.png
data/     site, narrators, sessions, segments, topics, assemblies, places, persons, mentions (.json)
          bundle.js — 파일로 바로 열 때 쓰는 묶음(자동 생성)
media/    세션 원본 영상(mp4)·자막(vtt)·표지, 하이라이트 클립, thumbs/ 세그먼트 화면
downloads/ 색인 포함 전문 PDF 표본(홍석진 편)
tools/    build_media.py(표본 영상·타임코드 생성), build_bundle.py(데이터 검증·묶음), build_pdf.py(전문 PDF), frame.html, voices.json
```

## 데이터를 고친 뒤 다시 만드는 순서

```
python3 tools/build_media.py    # 녹취문 → 합성 음성 → 타임코드·영상·자막 (ffmpeg, Chrome 필요, 약 2분)
python3 tools/build_bundle.py   # 참조 검증, persons[] 재계산, bundle.js 생성
python3 tools/build_pdf.py n01  # 색인 포함 전문 PDF
```

- 녹취문 문장을 고치면 build_media.py부터 다시 실행해야 타임코드가 맞음
- 실제 녹취문·영상을 넣는 2단계에서는 build_media.py 대신 STT·강제 정렬 파이프라인(ISMP PoC 「구술기록」)의 산출물이 `segments.json`의 `tc`·`start_tc`·`end_tc`를 채움

## 기획설계서 3.4 스키마 대비 추가·확장 항목

| 대상 | 항목 | 이유 |
| --- | --- | --- |
| session | `video_url`, `captions_url`, `poster_url`, `published_at` | 세그먼트가 가상 구간이므로 원본 영상은 회차 단위로 둠. 최근 공개 목록 |
| segment | `video_url` | 하이라이트 등급일 때만 물리 클립 경로. 그 외에는 null(회차 원본의 구간 재생) |
| segment | `clip_offset_tc` | 하이라이트 클립 0초가 원본의 몇 초인지. 녹취문 타임코드를 클립 시간으로 환산 |
| segment | `period` {from, to} | OH-05 시대 축(국회 대수 타임라인) 배치 |
| segment | `planned_duration` | 방문 열람 구간의 길이(녹취문 없이 타임라인 유지) |
| transcript_sync[] | `id` | 언급(mention)과 대표 인용이 문단을 가리키기 위함 |
| narrator | `person_id`, `headline_position`, `photo`, `full_transcript_pdf`, `is_sample` | 인명 사전·국회의원 컬렉션과 같은 인물 식별자, 카드 표기, 전문 PDF |
| 신규 | `places.json`, `persons.json`, `mentions.json` | 노션 기획안 8.7의 지명·인명 사전과 언급 스키마 |
| 신규 | `assemblies.json` | 국회 대수 기간(시대 축) |
| session | `record_no`, `summary`, `media`, `language`, `status` | 관리번호, 회차 요약(방문 열람·정리 중 구술의 온라인 게시분), 조건 검색 |
| segment | `keywords`, `hyperlinks`, `title_en`, `summary_en`, `transcript_sync[].text_en` | OHMS 방식 색인(키워드·관련 링크), 영문 병기 |
| narrator | `category`, `assembly_range` | 구술자 구분 탭, 재임 시기 정렬 |

## 알려진 한계

- 합성 음성은 Yuna 한 가지로 화자를 음높이·속도로만 구분함. 맥에 고품질 한국어 음성을 설치하면 `tools/voices.json`의 `voice`만 바꿔 다시 만들 수 있음
- Google 지도 화면은 API 키를 넣어야 실제 지도로 보임(키 없는 상태의 대체 화면으로 시험함)
- 공유 버튼은 링크·문구 복사만 함. 소셜 공유와 메타 태그는 개편 사업에서 구현
- 국회의원 컬렉션·회의록시스템·의안정보시스템 연결은 '연결 자리'만 표시함
